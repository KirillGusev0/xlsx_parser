# xlsx_parser
Сервис для импорта email-рассылок из XLSX файла с последующей отправкой писем.

Возможности:
  импорт данных из XLSX
  обработка больших файлов (streaming)
  защита от дубликатов через external_id
  валидация входных данных
  имитация отправки писем (log + random delay)
  статистика выполнения импорта

Основные компоненты:
  importer:  
    читает XLSX построчно
    валидирует данные
    выполняет дедупликацию
    сохраняет записи в БД
    возвращает статистику выполнения
  
  sender: 
    обрабатывает pending письма
    имитирует отправку
    обновляет статус записи

ТЗ: Необходимо реализовать механизм импорта рассылок из файла формата XLSX с последующей отправкой писем. Каждая строка файла содержит данные письма, которое должно быть отправлено пользователю. Файл может содержать большое количество записей.

Импорт должен запускаться через Django management command.

Первая строка файла содержит заголовки колонок.

Колонка	Описание
external_id	уникальный идентификатор записи во внешней системе
user_id	идентификатор пользователя
email	email получателя
subject	тема письма
message	текст письма
external_id используется для предотвращения повторной обработки одной и той же записи при повторном импорте файла.

После завершения работы команда должна вывести результат обработки, например:

количество обработанных строк;
количество созданных записей;
количество пропущенных записей;
количество ошибочных строк.
Примечание: для упрощения задачи отправка письма должна быть реализована как запись сообщения в лог с задержкой.

Примечание: данное задание рекомендуется выполнить в prod-like стиле, представьте, что это часть продуктовой разработки. Данное задание будет оцениваться не только с точки зрения корректности результата, но и соответствию качества кода (читаемость / соответствие стандартам разработки / документация / тесты).

Разместите ваше решение на GitHub, проект должен быть публично доступен и находиться в ветке main.

Требования к решению
Python 3.10+
Django 4.2+
решение должно быть оформлено как Django-проект;
база данных — SQLite или PostgreSQL;
проект должен запускаться локально.
